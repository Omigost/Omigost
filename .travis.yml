sudo: required

notifications:
  - email: false

services:
  - docker

install:
  - rm -r -f -d $HOME/.cache/yarn
  - rm -r -f -d node_modules
  - ./gradlew bootJar

script:
  - ./gradlew integrationTest
